version: "1"
project:
    id: ""
packages:
  - name: "db"
    path: "./db/sqlc"
    schema: "./db/migration/"
    queries: "./db/query/"
    engine: "postgresql"
    emit_prepared_queries: false
    emit_json_tags: true